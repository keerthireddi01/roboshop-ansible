- name: print a text
  hosts: all
  tasks:
    - name: print hellow lerf
      ansible.builtin.debug:
       msg: hello worls

    - name: get anothr file
      ansible.builtin.include_tasks: 
       file: sample.yaml   